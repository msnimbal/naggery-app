{
  "name": "naggery-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd app && npm install && npm run build",
    "start": "cd app && npm start",
    "dev": "cd app && npm run dev"
  },
  "engines": {
    "node": "18.x"
  }
}
